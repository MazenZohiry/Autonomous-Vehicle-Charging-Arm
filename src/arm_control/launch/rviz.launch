<?xml version="1.0"?>
<launch>

   <!-- launch control.launch -->
  <include file="$(find arm_control)/launch/control.launch"/>

  <!-- Rviz to vizualize robot -->
  <node name="rviz" pkg="rviz" type="rviz" output="screen" args="-d '$(find arm_control)/rviz/moveit.rviz'" />
  
</launch>
